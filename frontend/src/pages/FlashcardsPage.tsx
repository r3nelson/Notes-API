// Page showing all flashcards, and possibly a form for adding/editing
// Page showing all subjects, with options to add/edit subjects

import React from "react";
import FlashcardsDisplay from "../components/Flashcards/FlashcardDisplay";

const FlashcardsPage: React.FC = () => {
  return <FlashcardsDisplay />;
};

export default FlashcardsPage;
